apiVersion: v2
name: border-router
description: "Garuda border_router (local border egress + OSPF sidecar, no WireGuard)."
type: application
version: 0.4.0
appVersion: "0.1.0"

dependencies:
  - name: frr-sidecar
    version: "0.2.0"
    repository: "oci://ghcr.io/garuda-tunnel/charts"
