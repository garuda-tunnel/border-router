{
  "$schema": "https://json-schema.org/draft-07/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["namespace", "name", "images"],
  "properties": {
    "namespace": { "type": "string", "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" },
    "name":      { "type": "string", "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" },
    "images": {
      "type": "object",
      "additionalProperties": false,
      "required": [],
      "properties": {
        "border": { "type": "string" },
        "frr":    { "type": "string" }
      }
    },
    "frr-sidecar": { "type": "object" },
    "ospf": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["router_id"],
      "properties": {
        "router_id": { "type": "string" }
      }
    },
    "borderRouter": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "mssClampEnabled": { "type": "boolean" },
        "mssClampValue":   { "type": "integer", "minimum": 0, "maximum": 1460 }
      }
    }
  }
}
